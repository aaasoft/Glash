@inherits ComponentBase_WithGettextSupport

<table class="table">
    <thead>
        <tr>
            <th scope="col">@TextName</th>
            <th scope="col">@TextAgentName</th>
            <th scope="col">@TextStatus</th>
            <th scope="col">
                @TextOperate
                <div class="btn-group btn-group-sm" role="group">
                    <button type="button" title="@TextAdd" class="btn btn-secondary pt-0 pb-0" @onclick="Add">
                        <i class="fa fa-plus"></i>
                    </button>
                </div>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var t in Core.GlashAgentManager.Instance.ProfileContexts)
        {
            <tr>
                <td>@t.Model.Name</td>
                <td>@t.Model.AgentName</td>
                <td>@t.Status</td>
                <td>
                    <div class="btn-toolbar">
                        <div class="btn-group">
                            <button type="button" class="btn btn-sm btn-primary" @onclick="@(()=>ShowLogs(t.Model))">
                                @TextLogs
                            </button>
                            <button type="button" class="btn btn-sm btn-primary" @onclick="@(()=>Edit(t.Model))">
                                @TextEdit
                            </button>
                            <button type="button" class="btn btn-sm btn-danger" @onclick="@(()=>Delete(t.Model))">
                                @TextDelete
                            </button>
                        </div>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

<ModalWindow @ref="modalWindow" />
<ModalAlert @ref="modalAlert" />