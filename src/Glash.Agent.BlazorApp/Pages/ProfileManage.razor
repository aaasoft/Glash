<table class="table">
    <thead>
        <tr>
            <th scope="col">@Global.Instance.TextManager.GetText(Model.Profile.Texts.Name)</th>
            <th scope="col">@Global.Instance.TextManager.GetText(Model.Profile.Texts.AgentName)</th>
            <th scope="col">@Global.Instance.TextManager.GetText(Model.Profile.Texts.Status)</th>
            <th scope="col">
                @Global.Instance.TextManager.GetText(Texts.Operate)
                <div class="btn-group btn-group-sm" role="group">
                    <button type="button" title="@Global.Instance.TextManager.GetText(Texts.Add)" class="btn btn-secondary pt-0 pb-0" @onclick="Add">
                        <i class="fa fa-plus"></i>
                    </button>
                </div>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var t in Core.GlashAgentManager.Instance.ProfileContexts)
        {
            <tr>
                <td>@t.Model.Name</td>
                <td>@t.Model.AgentName</td>
                <td>@t.Status</td>
                <td>
                    <div class="btn-toolbar">
                        <div class="btn-group">
                            <button type="button" class="btn btn-sm btn-primary" @onclick="@(()=>Edit(t.Model))">
                                @Global.Instance.TextManager.GetText(Texts.Edit)
                            </button>
                            <button type="button" class="btn btn-sm btn-danger" @onclick="@(()=>Delete(t.Model))">
                                @Global.Instance.TextManager.GetText(Texts.Delete)
                            </button>
                        </div>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

<ModalWindow @ref="modalWindow" />
<ModalAlert @ref="modalAlert" />